# ============================================================
# MOF CO2/H2 Screening Pipeline — pip requirements
# Manuscript: Digital Discovery (resubmission)
# Author: Md. Rifat Khandaker (rifatkh.duet@gmail.com)
# ============================================================
# Install: pip install -r requirements.txt
# Recommended: use conda environment (environment.yml) instead
#              for full reproducibility including RDKit and Zeo++
# ============================================================

# Core ML
xgboost>=2.0
lightgbm>=4.0
scikit-learn>=1.3
optuna>=3.3

# Data handling
pandas>=1.5
numpy>=1.24
pyarrow>=12.0
scipy>=1.11

# Interpretability
shap>=0.42

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Chemistry (pip versions — prefer conda-forge for RDKit)
pymatgen>=2023.8

# Utilities
tqdm>=4.65
joblib>=1.3

# ============================================================
# NOTE: RDKit and Zeo++ are not reliably installable via pip
# on all platforms. Use the conda environment (environment.yml)
# if you need synthesizability scoring or geometric feature
# computation. All scripts live at the repository root (no
# scripts/ subfolder) — see README.md "Repository structure".
# ============================================================
